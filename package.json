{
  "name": "sistema-reservas-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build && node -e \"require('fs').copyFileSync('CNAME', 'dist/CNAME')\"",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^10.8.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.4.21",
    "gh-pages": "^6.0.0"
  }
}